<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layoutCallSms"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="20dp"
    android:background="@color/white">

    <!-- Tiêu đề -->
    <TextView
        android:text="📞 Gọi điện & Nhắn tin"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/purple_700"
        android:layout_marginBottom="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- Ô nhập số điện thoại -->
    <EditText
        android:id="@+id/txtPhone"
        android:hint="Nhập số điện thoại"
        android:inputType="phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:backgroundTint="@color/purple_700" />

    <!-- Ô nhập nội dung tin nhắn -->
    <EditText
        android:id="@+id/txtMessage"
        android:hint="Nhập nội dung tin nhắn"
        android:inputType="textMultiLine"
        android:minLines="3"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:backgroundTint="@color/purple_700" />

    <!-- Nút gọi -->
    <Button
        android:id="@+id/btnCall"
        android:text="📞 Gọi điện thoại"
        android:textSize="18sp"
        android:backgroundTint="@color/purple_700"
        android:textColor="@color/white"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Nút gửi SMS -->
    <Button
        android:id="@+id/btnSms"
        android:text="✉️ Gửi tin nhắn"
        android:textSize="18sp"
        android:backgroundTint="@color/purple_700"
        android:textColor="@color/white"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Nút quay lại -->
    <Button
        android:id="@+id/btnBack"
        android:text="⬅️ Quay lại màn hình chính"
        android:textSize="18sp"
        android:backgroundTint="@color/purple_700"
        android:textColor="@color/white"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- 🧭 Icon gọi điện nhanh -->
    <ImageView
        android:id="@+id/imgCallIcon"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:layout_marginTop="25dp"
        android:src="@drawable/call"
        android:contentDescription="Gọi điện nhanh"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
